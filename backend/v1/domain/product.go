package domain

// ProductVersion is the only Nomen release identifier until Vault, Mesh, and
// the first IAM vertical slice are operational. Do not mint 1.1, v2 APIs, or
// a second product name while this is frozen.
const ProductVersion = "1.0.0-alpha"
